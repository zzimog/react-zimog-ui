import { User } from 'lucide-react';

# React UI

Work in progress React UI personal project.

Use the _Hamburger_ menu in the top left corner to open the menu and navigate through the available components.